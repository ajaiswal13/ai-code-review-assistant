spring:
  application:
    name: ai-code-review-assistant

  datasource:
    url: jdbc:postgresql://localhost:5432/ai_review_db
    username: ai_user
    password: ai_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

    open-in-view: false

    properties:
      hibernate:
        format_sql: true

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}

logging:
  level:
    root: INFO

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000